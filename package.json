{
  "name": "gcli2api-go",
  "private": true,
  "type": "module",
  "description": "Admin UI and static assets tooling for gcli2api-go",
  "version": "0.0.0",
  "scripts": {
    "lint": "eslint web --ext .js",
    "lint:fix": "eslint web --ext .js --fix",
    "typegen": "openapi-typescript docs/openapi/openapi.yaml --output web/types/openapi.d.ts",
    "typecheck": "cd web && npm run build:ts"
  },
  "devDependencies": {
    "eslint": "^9.12.0",
    "eslint-plugin-import": "^2.29.1",
    "openapi-typescript": "^7.5.2"
  }
}
